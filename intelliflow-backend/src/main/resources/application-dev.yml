spring:
  jpa:
    show-sql: true
    properties:
      hibernate:
        format_sql: true

logging:
  level:
    root: INFO
    com.intelliflow: DEBUG
    org.springframework.security: DEBUG
    org.hibernate.SQL: DEBUG
